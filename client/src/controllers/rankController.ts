const requestRegions = async (
  address: string,
  scope: 'big' | 'medium' | 'small',
) => {};

const requestBigRegions = async () => {};

const requestMediumRegions = async (address: string) => {};

const requestSmallRegions = async (address: string) => {};

export { requestBigRegions, requestMediumRegions, requestSmallRegions };
